[build]
  command = "npm run build"
  publish = "dist"

# SPA: todas las rutas sirven index.html (Vue sin router de rutas, pero útil para recargas)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
